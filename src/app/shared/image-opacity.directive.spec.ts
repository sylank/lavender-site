import { ImageOpacityDirective } from './image-opacity.directive';

describe('ImageOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
